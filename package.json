{
  "name": "lasvegasmobilenotaryservices",
  "version": "1.0.0",
  "description": "Lead-gen static site for lasvegasmobilenotaryservices.com — Eleventy 3.x on Cloudflare Pages, GHL-integrated.",
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "dev:worker": "npm run clean && eleventy && wrangler dev",
    "deploy": "npm run clean && eleventy && wrangler deploy",
    "clean": "node -e \"require('node:fs').rmSync('_site',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "wrangler": "^4.111.0"
  },
  "dependencies": {
    "notary-lead-form": "github:EmperorFPI/notary-lead-form#semver:^1.0.0"
  }
}
